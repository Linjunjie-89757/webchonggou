# Verification And Risk Register

This file tracks known unverified paths and residual risks during the frontend rebuild. Update it after each goal when a path is intentionally left for later regression.

## Current Policy

- Each feature goal runs lightweight validation: `npm.cmd run typecheck`, `npm.cmd run build`, and focused browser smoke checks when useful.
- Full Playwright regression is reserved for module stabilization goals.
- Avoid real create/update/delete operations on production-like data unless test data or explicit approval is available.
- Backend compile is only required when backend files are changed.

## Global Risks

| Area | Status | Risk | Follow-up |
| --- | --- | --- | --- |
| Build warnings | Open | `@vueuse/core` PURE annotation and chunk size over 500k still appear. Build passes. | Revisit during final build optimization. |
| Git workspace | Open | Outer `WODEZIDONGHUA` remains dirty and intentionally ignored. | Do not stage unless user explicitly asks. |
| Build output | Open | Local `dist` is regenerated by build and not submitted. | Keep untracked/ignored behavior; never stage build output. |
| Backend compile | Open | Earlier backend compile was blocked by unrelated `ApiExecutionEngineSupport.readScenarioSteps` error. | Recheck when backend work resumes. |
| Encoding | Monitoring | PowerShell may display UTF-8 Chinese as mojibake, while source files are UTF-8. | Prefer editor/file checks over console display for Chinese text. |

## Config Center

| Area | Not Fully Verified | Risk | Follow-up |
| --- | --- | --- | --- |
| Real-data destructive actions | Delete/status/test actions were mostly validated as stable failure or smoke paths. | Real success paths may still hide payload or permission issues. | Add targeted real-data tests with disposable records. |
| Full UI parity | Only local alignment and stability fixes were done. | Old-project visual differences may remain. | Run the planned UI parity pass later. |
| 401 during active form save | Hard to reliably manufacture expired session during modal save. | Some edge cases may still show generic handling. | Add mock/session-expiry Playwright scenario. |

## Case Center

| Area | Not Fully Verified | Risk | Follow-up |
| --- | --- | --- | --- |
| Real destructive operations | Delete/status/batch paths were not exhaustively run against real data. | Real data mutation can expose permission or payload issues. | Use seeded test cases for full regression. |
| Batch update success/failure matrix | Only focused paths were checked. | Selection clearing and partial failures may need more coverage. | Include in case-center stabilization regression. |
| UI final parity | Initial alignment was done before final module-wide parity. | Table density and operation menus may need old-project comparison. | Run the planned UI parity pass. |

## Defect Management

| Area | Not Fully Verified | Risk | Follow-up |
| --- | --- | --- | --- |
| Create/edit success | Dialogs and validation were smoke-tested; real save success was skipped to avoid data pollution. | Backend validation, assignee rules, and related case payload issues may appear on real save. | Test with disposable defect records. |
| Assignee selector | No reusable user-list API was found, so `assigneeId` is numeric input. | User experience is rough and invalid IDs rely on backend failure. | Add real user selector once user API is confirmed. |
| Detail description rendering | Description is displayed as plain text. | Existing records may contain HTML strings and appear with tags. | Decide whether to render sanitized HTML or keep plain text. |
| Assign success | Assign dialog validation was checked; real success request was skipped. | Permission or payload issues may remain. | Validate with disposable record and known user ID. |
| Transition success | Transition dialog opening was checked; real success request was skipped. | Backend may reject invalid state transitions. | Confirm allowed transition matrix and test real allowed transitions. |
| Transition options | Frontend currently shows all statuses. | User can choose a status the backend does not allow from current state. | Restrict options after backend contract is confirmed. |
| Failure mock paths | No mocked 500/401 assign-transition failures were run. | Some failure UI edge cases may remain. | Add focused mock failure smoke tests. |
| Full module regression | Only lightweight smoke checks have been run for latest goals. | Interactions between create/edit/detail/assign/transition may hide regressions. | Run defect stabilization goal after feature set is complete. |

## Recommended Next Regression Goals

1. Defect management stabilization: full smoke plus real-data issue list for read, filter, create/edit, detail, assign, transition.
2. Defect UI parity: compare density, operation menu, dialogs, drawer, empty/error states against old frontend.
3. Cross-module session regression: login, 401, logout, protected route redirect, and modal save failure behavior.

