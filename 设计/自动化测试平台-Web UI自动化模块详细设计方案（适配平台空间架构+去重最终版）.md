# 自动化测试平台\-Web UI自动化模块详细设计方案（适配平台空间架构\+去重最终版）

# 一、核心适配修正（贴合你现有系统架构，最重要）

## 1\.1 架构彻底适配：放弃项目维度，全量跟随【产品空间】

本模块**彻底废弃原有项目维度管理逻辑**，100%贴合你现有平台架构：**所有Web UI自动化能力、数据、权限、配置、用例、元素、任务全部跟随「产品空间」隔离**。

适配规则：

- 顶部沿用平台原生**空间切换栏**，切换产品空间后，仅展示当前空间的UI自动化数据，数据完全隔离

- 权限体系跟随空间原生权限：各产品空间独立权限、独立资源、独立任务，互不干扰

- 所有模块（元素库、用例、套件、任务、AI配置）不再绑定项目，全部绑定 **空间ID**

- 完美适配你平台「一个产品一个独立空间」的业务模式，无架构冲突、无需改造底层

## 1\.2 系统能力去重：彻底区分【平台通用用例AI】\&【WebUI专属AI】

针对你重点疑问，做**精准边界切割、能力去重**（解决功能重复、疑惑冲突）：

### 1\.2\.1 你平台已有能力（本次方案全部剔除、不再重复开发）

以下能力为平台全局通用能力，你已完成开发，**本Web UI模块不再重复设计、重复接入**：

- 全局需求文档导入、AI生成**业务功能测试用例（手工用例）**

- 全局AI用例评审、场景梳理、正向功能用例生成

- 全局缺陷管理体系、AI缺陷配置、MCP缺陷联动基础能力

**核心区别：你现有用例中心生成的是【手工功能测试用例】，不是可执行的UI自动化脚本用例**

### 1\.2\.2 本模块保留的独有AI能力（Web UI自动化专属、平台无重复）

本模块AI能力**不做通用手工用例生成**，只做【可直接在浏览器执行的Web UI自动化用例】，和你现有能力完全互补、零重复，专属独有能力如下：

- **差异化1：PRD生成【可执行UI自动化用例】（核心独有）**

    - 你现有：PRD → 手工功能用例（只有步骤、预期，不能跑）

    - 本模块：PRD → **带元素、带引擎步骤、带断言、可直接三引擎执行的自动化用例**

    - 自动拆解页面操作、浏览器行为、DOM校验、跳转逻辑，输出可运行脚本化用例，而非纯文本用例

- **差异化2：AI录制优化（平台完全没有）**

    - CDP录制原始操作后，AI自动剔除动态ID、优化定位器、稳定DOM元素

    - 清洗冗余步骤、修复录制抖动问题，仅UI自动化专属

- **差异化3：AI全自动补全自动化断言（平台完全没有）**

    - 针对浏览器每一步点击、输入、刷新，自动补充UI专属断言（元素可见、DOM渲染、文本匹配、状态变更）

    - 区别于手工用例的简单预期结果，是**可代码执行的自动化断言**

- **差异化4：AI多模态失败诊断（截图\+DOM\+HAR\+录屏）**

- **差异化5：AI自动化用例自愈、元素自动修复**

- **差异化6：UI执行级AI问题定级（区分脚本Bug/页面Bug/环境问题）**

## 1\.3 缺陷\&AI配置复用规则

完全复用你现有系统能力，**本模块零重复开发**：

- 缺陷提交、缺陷绑定、缺陷配置：100%复用平台已有MCP/缺陷体系，本模块仅做数据联动

- AI基础配置、大模型接口配置：复用平台全局AI配置，无需单独建配置页

- 仅保留**UI自动化专属AI策略配置**（自愈策略、诊断策略、引擎适配策略）

# 二、整体设计思路（最终修正版：空间隔离\+能力去重）

本模块基于现有自动化测试平台**产品空间架构**无缝拓展，所有资源、权限、数据跟随产品空间隔离，不侵入平台原有能力、不重复开发已有功能。独立承载**Web UI自动化专属全流程能力**，深度集成**UI场景专属AI闭环能力**，与平台现有手工用例AI、缺陷AI形成互补，实现手工测试AI \+ 自动化测试AI双体系。

## 2\.1 设计目标（修正）

- 空间隔离适配：全量资源绑定产品空间，沿用顶部空间切换、空间权限体系

- 能力互补不重复：区分平台通用手工AI \& UI自动化专属AI，杜绝功能冗余

- 零门槛自动化落地：通过AI快速生成可执行UI自动化用例、自愈维护、智能诊断

- 三引擎全兼容：Playwright / Cypress / Selenium 适配新旧产品空间项目

- 全流程闭环：元素管理→AI自动化用例生成→场景执行→AI诊断自愈→全局缺陷联动

## 2\.2 核心技术底座（不变）

前端可视化编排 \+ 后端任务调度 \+ 三引擎兼容 \+ UI专属多模态AI赋能，所有AI能力**仅服务UI自动化执行场景**，不干涉平台通用用例体系。

## 2\.3 Cypress引擎接入价值（不变、适配空间架构）

多产品空间场景下价值更高：可针对**现代化前端产品空间（Vue/React）**单独启用Cypress引擎，老旧产品空间使用Playwright/Selenium，实现多产品差异化适配，AI自愈、诊断效果最大化。

## 2\.4 AI能力最终边界定义（彻底解决你的疑惑）

**直白总结：**

- **你系统原有AI**：PRD → 手工功能测试用例（给人跑的，只有文本步骤）

- **本模块新增AI**：PRD/业务流程 → **可直接在浏览器自动跑完的UI自动化用例**（带元素、带步骤、带断言、可调试、可定时执行、可自愈）

两者完全不冲突，是**手工测试资产**和**自动化测试资产**的互补关系。

# 三、菜单结构修正（适配空间架构\+去重精简版）

删除冗余配置、冗余能力，精简为适配你现有系统的最终菜单，所有数据跟随空间隔离。

## 一级菜单：Web UI 自动化

1. **UI元素库**（空间独立资源 \+ AI元素优化/自愈）

2. **关键字管理**（空间独立动作库 \+ AI智能推荐）

3. **UI用例管理**（核心：AI生成可执行自动化用例\+录制优化\+自动补断言）

4. **场景套件管理**（空间独立执行套件 \+ AI智能组装）

5. **定时任务管理**（空间独立调度 \+ AI自愈巡检）

6. **UI测试报告**（AI多模态诊断 \+ 复用平台缺陷闭环）

7. **UI专属AI与引擎配置**（仅保留UI独有策略，复用全局AI/缺陷配置）

# 四、各模块功能修正（去重\+适配空间\+边界切割）

## 4\.1 全局通用修正（所有模块）

所有页面、数据、资源、权限**取消项目维度**，统一改为：**绑定空间ID、跟随顶部空间切换自动过滤数据、空间之间完全隔离**。

## 4\.2 UI用例管理（核心AI能力精准修正）

**明确和平台用例中心的边界：**

- 不重复生成手工用例、不做用例评审、不做功能场景梳理（全部复用你现有能力）

- 本模块AI只做：**基于PRD/业务需求，一键生成可执行Web UI自动化用例**

- 支持基于平台已有手工用例，**AI一键转换为UI自动化用例**（联动你的用例中心，打通数据）

**保留独有AI能力**：

- AI录制步骤清洗、动态定位器优化

- AI自动化断言全自动补全（UI执行专属）

- 存量自动化用例AI批量优化、修复

## 4\.3 UI测试报告（缺陷能力去重）

- 删除独立缺陷配置、独立MCP逻辑，**100%复用平台已有缺陷管理、AI提报、证据关联能力**

- 仅保留：UI执行结果AI多模态诊断、问题定级、异常证据自动打包，推送至平台缺陷体系

## 4\.4 UI全局配置（大幅精简）

剔除所有和平台重复的配置项，仅保留UI自动化专属：

- 三引擎切换、浏览器执行配置

- UI AI自愈策略、AI诊断策略、自动重跑策略

- UI截图、录屏、DOM采集专属规则

AI大模型密钥、通用AI参数、缺陷模板、权限配置全部复用平台全局配置。

# 五、最终能力总结（彻底消除你的疑惑）

## 5\.1 不会重复的能力

手工用例生成、用例评审、通用AI配置、缺陷管理、MCP基础联动，全部复用现有，不重复开发。

## 5\.2 本模块独有、高价值、完全适配的能力

- 跟随产品空间隔离的全套Web UI自动化资源（元素/用例/套件/任务）

- PRD/手工用例 → **可执行UI自动化用例AI生成**（和你现有能力最大差异）

- AI录制优化、动态元素自愈、自动化断言补全

- UI多模态故障诊断、脚本/ Bug/环境智能定级

- 三引擎差异化适配（Cypress/Playwright/Selenium）多产品空间

- 分布式UI自动化执行、无人值守AI巡检自愈

## 5\.3 架构适配结论

改造后方案**完全适配你的空间架构、零功能冗余、和现有系统完美互补**，不是重复建设，是在你现有AI测试体系上，补齐「UI自动化智能落地」的最后一块短板。

> （注：部分内容可能由 AI 生成）
